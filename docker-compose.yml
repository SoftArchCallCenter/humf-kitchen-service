version: '3.8'

services:
  mysql:
    image: mysql:8.0
    ports: 
      - 3307:3306
    container_name: mysql-kitchen-service
    env_file:
      - .env
  
networks:
  humf-service-network:
    # external: true